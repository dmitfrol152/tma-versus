// import { useMemo } from "react";
// import type { MaxValueDataProps } from "../types";

// export function useMaxValue({ dataDefault }: MaxValueDataProps) {
//   const maxValue = useMemo(() => {
//     const status = dataDefault.treader.title;
//     if (status === "basic") {
//       return 3000;
//     }
//     return 3000;
//   }, [dataDefault.treader.title]);

//   return maxValue;
// }
